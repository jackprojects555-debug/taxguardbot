services:
  - type: worker
    name: taxguardbot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app/bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: DATABASE_URL
        sync: false
